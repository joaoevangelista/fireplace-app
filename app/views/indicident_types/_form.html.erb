<%= form_for(@indicident_type) do |f| %>
  <% if @indicident_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indicident_type.errors.count, "error") %> prohibited this indicident_type from being saved:</h2>

      <ul>
      <% @indicident_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
